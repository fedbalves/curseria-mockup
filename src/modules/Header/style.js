import styled from 'styled-components';
import Theme from '../Theme';

export const StyledHeader = styled.header`
  display: block;
  position: relative;
  height: 90px;
  background-color: ${Theme.colors.bases[300]};
  border-bottom: 1px solid ${Theme.colors.bases[100]};
`
